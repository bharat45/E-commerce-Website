<%- include('./partials/header') %>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<div style="margin-top: 35px;" class="text-center">
			<h2>CheckOut</h2>
			<table class="bill table-center">
				<tr class="bill">
					<th class="bill">Product</th>
					<th class="bill">Price</th>
					<th class="bill">Quantity</th>
					<th class="bill">Total</th>
				</tr>
				<tr class="bill">
					<td class="bill"><%= product.name %></td>
					<td class="bill"><%= product.price %></td>
				    <td class="bill"><%= quantity %></td>
				    <td class="bill"><%= quantity*product.price %></td>
				</tr>
				<tr class="bill">
					<td class="bill">-</td>
					<td class="bill">-</td>
					<td class="bill">-</td>
					<td class="bill"><%= quantity*product.price %></td>
				</tr>
			</table>
			<form action="/<%= user.id %>/checkout2/<%= product.id %>/<%= quantity %>" method="POST">
				<button class="btn btn-success btn-md">Place Order</button>
			</form>
		</div>
	</div>
</div>

<%- include('./partials/footer') %>